show=drun
prompt=Search

# Spotlight sits slightly above centre rather than dead middle
width=680
height=420
location=center

allow_images=true
image_size=32

insensitive=true
matching=fuzzy
no_actions=true
hide_scroll=true

term=ghostty

# the stylesheet is a dark one
gtk_dark=true
